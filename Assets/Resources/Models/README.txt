Put imported FBX files or prefabs here.

Unity builds can load Resources assets at runtime, but they cannot import
arbitrary external FBX files directly. Convert PLATEAU or other source data
to FBX in another Unity project first, then copy the imported FBX/prefab
asset into this folder.
